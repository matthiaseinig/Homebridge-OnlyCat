{
  "pluginAlias": "OnlyCat",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Homebridge plugin for the OnlyCat smart cat flap. Generate a token in the OnlyCat mobile app and paste it below.",
  "footerDisplay": "Source & docs: https://github.com/matthiaseinig/OnlyCatHomebridge",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "OnlyCat"
      },
      "token": {
        "title": "OnlyCat API Token",
        "type": "string",
        "required": true,
        "format": "password",
        "description": "API token from the OnlyCat mobile app (Settings → Developer)."
      },
      "debug": {
        "title": "Verbose logging",
        "type": "boolean",
        "default": false,
        "description": "Log raw Socket.IO traffic. Leave off for normal use."
      },
      "ffmpegPath": {
        "title": "ffmpeg path",
        "type": "string",
        "description": "Optional override for the ffmpeg binary used by the camera and HKSV pipelines. Leave blank to resolve via PATH."
      },
      "unlockPolicyName": {
        "title": "Policy to activate when unlocking",
        "type": "string",
        "description": "Name of the OnlyCat transit policy to activate when the HomeKit Cat Flap lock is unlocked. Case-insensitive. Leave blank to auto-pick the first policy with idleLock=false."
      },
      "lockPolicyName": {
        "title": "Policy to activate when locking",
        "type": "string",
        "description": "Name of the OnlyCat transit policy to activate when the HomeKit Cat Flap lock is locked. Case-insensitive. Leave blank to auto-pick the first policy with idleLock=true."
      },
      "disableCamera": {
        "title": "Disable camera service",
        "type": "boolean",
        "default": false,
        "description": "Suppress the camera and HomeKit Secure Video on each flap. Sensors, lock, and switches still appear, but as separate tiles in the Home app. Leave off unless you specifically don't want camera support."
      },
      "loopSlate": {
        "title": "Show divider between live-view replay loops",
        "type": "boolean",
        "default": true,
        "description": "Live view replays the most recent cat-flap event clip continuously. With this on, a brief black slate plays between iterations so it's clear you're seeing the same event repeating, not a continuous live feed. Turn off for a seamless loop."
      },
      "replayHistoryOnStartup": {
        "title": "Replay event history on startup (days)",
        "type": "integer",
        "default": 0,
        "minimum": 0,
        "maximum": 30,
        "description": "When > 0, fetch events from the last N days on plugin startup and push them through HomeKit Secure Video. The clip content is correct, but HomeKit will timestamp these recordings at the moment of replay, not the original event time - that's an Apple HKSV API limitation we can't bypass. Leave at 0 for fresh-from-install behaviour."
      }
    }
  }
}
